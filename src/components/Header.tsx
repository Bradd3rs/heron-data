import { Flex, Heading } from '@chakra-ui/react';

const Header = () => {
  return (
    <Flex padding="0.5rem" width="100%">
      <Heading>Header</Heading>
    </Flex>
  );
};

export default Header;
